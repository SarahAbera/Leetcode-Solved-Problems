class Solution:
    def findDuplicates(self, nums: List[int]) -> List[int]:
        frequency = Counter(nums)
        ans = []
        for key,val in frequency.items():
            if val == 2:
                ans.append(key)
        return ans
